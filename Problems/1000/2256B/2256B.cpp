#include <iostream>

using namespace std;

int main() {
    int amt_problems, num_chars;
    string input;
    cin >> amt_problems;

    for(int i = 0; i < amt_problems; i++) {
        cin >> num_chars >> input;

        if(num_chars%2==0) {

        }
    }
}